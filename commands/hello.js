
// const memeLib = require('nodejs-meme-generator');
// const random = require("something-random-on-discord").Random

// const memeGenerator = new memeLib({
//   canvasOptions: { // optional
//     canvasWidth: 500,
//     canvasHeight: 500
//   },
//   fontOptions: { // optional
//     fontSize: 46,
//     fontFamily: 'impact',
//     lineHeight: 2
//   }
// });

async function printHello () {
       // let post;
       // let data = await random.getMeme()
       // message.channel.send(data)
       // memeGenerator.generateMeme({
       //        // you can use either topText or bottomText
       //        // or both of them at the same time
       //        topText: 'Meme',
       //        bottomText: 'Generator',
       //        // for local image you can use path 'img/folder/image.jpg'
       //        url: 'https://i.imgur.com/7FHoSIG.png'
       //      }).then(function(data) {
       //        res.set('Content-Type', 'image/png');
              
       //        res.send(data);
       //        post=data;
       //        return post;
       //      })

        // keyword 'return' is crucial here to send back the string when the function is called.
}

module.exports.printHello = printHello; // this line exports the above code so you can import it into the 'index.js' file for use.